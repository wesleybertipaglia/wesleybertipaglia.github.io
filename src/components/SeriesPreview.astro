---
import type { CollectionEntry } from 'astro:content';
import { textUtils } from '@lib/text-utils';
import ArrowOutwardIcon from '@icons/ArrowOutwardIcon.astro';

type Props = {
  series: CollectionEntry<'series'>;
};

const { series } = Astro.props;
---

<a
  href={`/series/${series.slug}`}
  class="post relative group flex flex-nowrap py-3 px-4 pr-10 rounded transition-colors duration-300 ease-in-out border
    border-light-border-3/10 dark:border-dark-border-3/10
    hover:bg-light-background-3/10 dark:hover:bg-dark-background-3/10"
>
  <div class="flex flex-col flex-1 truncate">
    <div class="font-semibold">
      {series.data.title}
    </div>
    <div class="text-sm text-light-text-3/75 dark:text-dark-text-3/75">
      {textUtils.excerpt({ text: series.data.description, maxLength: 64 })}
    </div>
  </div>
  <ArrowOutwardIcon
    className="absolute top-1/2 right-2 -translate-y-1/2 size-5 stroke-2 fill-none stroke-current"
  />

  <div class="tags hidden">
    {series.data.tags.join(', ')}
  </div>
</a>
