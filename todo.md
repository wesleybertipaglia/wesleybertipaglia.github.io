# TODO

- [ ] add claps feature
- [ ] the pagination isnt working, refactor the pagination, move all the logic to the utils, you can use the query-utils.ts as reference, you can use a similar logic to set and retrive the params (just page and size) in the page load get the params from the url and filter the results
